# cold-turkey-block-older-versions-installer

Newer versions of cold turkey block use database encryption (presumably after v4.5), you may revert to version 4.3 or 4.4 instead to avoid this.

And if you're interested, you can upgrade to Pro by running `script.py` or using tools like DB Browser to modify the local database.
